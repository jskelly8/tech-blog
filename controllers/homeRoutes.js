// Imports
const router = require('express').Router();
const { User, Post, Comment } = require('../models');
const withAuth = require('../utils/auth');

// Render Homepage Route

// Render Login Route

// Render Signup Route

// Render Dashboard (all blogs by current user)

// Render specific blog Route

// Render add-new post Route

// Render edit-existing post Route


// Export
module.exports = router;